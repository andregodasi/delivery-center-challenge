import styled from 'styled-components';

export const Container = styled.main`
  .orders__container-orders {
    margin-top: 1.5rem;
  }

  .orders__divider {
    margin: 0.3rem 0 1rem;
  }

  .orders__subtitle {
    font-size: 0.825rem;
    margin-top: 2rem;
    display: block;
  }

  .orders__pagination {
    display: flex;
    justify-content: center;
    padding: 1.2rem 0;
  }
`;
